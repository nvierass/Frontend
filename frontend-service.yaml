apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
   - name: http
     protocol: TCP
     port: 31000
     targetPort: 31000
     nodePort: 30001
